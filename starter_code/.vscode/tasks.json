{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-java-without-test",
            "type": "shell",
            "command": "mvn clean install -Dmaven.test.skip=true",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$maven"],
            "detail": "Build the project using Maven"
        }
    ]
}